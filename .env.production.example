# Copy values when building for production (do not commit real secrets).
# PowerShell:
#   $env:VITE_API_URL="https://api.mike.haddad.co.il"
#   npm run build
#   npx wrangler deploy --config dist/server/wrangler.json

VITE_API_URL=https://api.mike.haddad.co.il
